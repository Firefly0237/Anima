# Test Fixtures

`reward_toy_public.jsonl` contains tiny hand-authored toy/public records for schema and JSONL loader tests only.

They are not the formal first-20 human gold records, are not a training dataset, and must not be used as reported reward data. The formal 20 gold examples still require human authorship and final human review.

